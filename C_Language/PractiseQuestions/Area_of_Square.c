// Write a program to calculate area of a square.
#include <stdio.h>
int main()
{
    int side;
    printf("Enter side of square : ");
    scanf("&d", side);
    printf("Area of given square of side %d", side, " is ", side * side);
}